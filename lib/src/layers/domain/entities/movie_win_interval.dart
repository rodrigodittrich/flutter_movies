import 'movie_win_min_max_interval.dart';

class MovieWinInterval {
  List<MovieWinMinMaxInterval> min;
  List<MovieWinMinMaxInterval> max;

  MovieWinInterval({
    this.min = const [],
    this.max = const [],
  });
}