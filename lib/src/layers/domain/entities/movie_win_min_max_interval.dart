class MovieWinMinMaxInterval {
  String producer;
  int interval;
  int previousWin;
  int followingWin;

  MovieWinMinMaxInterval({
    this.producer = '',
    this.interval = 0,
    this.previousWin = 0,
    this.followingWin = 0,
  });
}